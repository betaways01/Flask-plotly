# uncomment to install missing packages
# pip install -r requirements.txt

!pip install flask
!pip install json

'''
the following are mostly present. However, if missing, uncomment too.
'''

!pip install pandas
#!pip install plotly
#!pip install plotly
#!pip install xlrd
#!pip install openpyxl
